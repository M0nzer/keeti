     token without expires DONE

     change the path to get it from query (upload) 

     change the select where (true & false out) DONE

     remove the query logs DONE

     but the token in a query DONE


     ============================================================
                 DONE       MUST DO FIRST       DONE
     ============================================================

     check for token in the url
     if token
        verify it from the database
            if verifed
                send response {id (USER ID) , username (USER PHONE NUMBER) , token , (THE NEW TOKEN) }
            else
                send response { message: 'Authentication error!' }
     else
        creat new token and save it into the database and THEN send response {id (USER ID) , username (USER PHONE NUMBER) , token , (THE NEW TOKEN) }
        u d true false
        